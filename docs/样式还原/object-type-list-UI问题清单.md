# object-type-list UI 问题清单

> **页面**：对象类型列表页面
> **设计稿**：Stitch `projects/11214155578152749288`；`docs/ui/对象类型列表页.png`
> **实现页与视口**：`http://127.0.0.1:5174/object-types`，1600 x 807
> **浏览器工具**：Codex in-app Browser 工具未暴露；按规则降级 Playwright CLI
> **验收证据**：`docs/样式还原/验收截图/pw-object-type-list-default.png`
> **证据保留策略**：仅保留最终引用截图；Playwright 中间快照位于 `.playwright-cli/`
> **轻量流程度量**：设计源类型 Stitch + docs-ui；最终证据 1 张；Browser 已降级；P0/P1/P2 当前剩余 0/0/0；验证命令通过

## 一、按区域列问题

| 优先级 | 区域 | 问题 | 状态 | 证据 |
|--------|------|------|------|------|
| P1 | 标题与分页数量 | 默认 mock 总数曾显示为样例行数 9，设计稿为 39。 | 已修复并重新截图 | `pw-object-type-list-default.png` |

## 二、回归结论

- 标题数量、左侧对象类型数量和分页总数均显示 39。
- 表格列顺序为类型名称、状态、可见性、修改时间、操作。
- 状态标签、可见性标签、详情链接、刷新/设置/创建按钮完整。
- 当前无 P0 阻断问题。
